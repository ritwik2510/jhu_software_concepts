# Module 7 — S3 → SageMaker Pipeline & EC2 Deployment

## Part 1: Running the notebook

1. Install dependencies: pip install -r requirements.txt

2. Configure AWS credentials on system: aws configure

3. Open "grad_cafe_pipeline.ipynb" in Jupyter and run the cells

4. The notebook will import the boto3 logic from s3_fetch.py.

Both score 10/10.

## Part 2: EC2 deployment

See ec2/EC2_DEPLOYMENT.md for full steps.

## AWS resources

Both the SageMaker notebook instance and the EC2 instance were stopped
after completing and verifying this assignment.
